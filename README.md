# School Management App

A Spring Boot application for managing schools and students. It allows users to track student grades and automatically categorizes them as Passed (>50%) or Failed (≤50%).

## Tech Stack
- **Backend:** Java 21, Spring Boot 3, Spring Data JPA
- **Database:** MySQL
- **Frontend:** HTML, JavaScript (Vanilla), CSS

## Setup & Run
1. Create the database:
   ```sql
   CREATE DATABASE school_db;